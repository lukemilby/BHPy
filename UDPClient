import socket

target_host = "127.0.0.1"
target_port = 80

#create socket
client = socket.socket(socket.AF_INET, socket.SOCK_DGRM)

#connect the client
client.connect("AAAABBBBCCCC"(target_host,target_port))

#receive some data
data, addr = client.recvfrom(4096)

print data